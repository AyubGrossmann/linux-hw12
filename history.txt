git branch -M main
git remote add origin https://github.com/AyubGrossmann/linux-hw12.git
git push -u origin main

git checkout -b feature

echo "# Linux HW12" >> README.md
echo "Feature branch changes" >> README.md

echo "__pycache__/" >> .gitignore
echo "*.pyc" >> .gitignore

git add README.md .gitignore
git commit -m "feature changes"
git push origin feature

git checkout main
git merge feature
git push origin main

history > history.txt
git add history.txt
git commit -m "add history file"
git push origin main
